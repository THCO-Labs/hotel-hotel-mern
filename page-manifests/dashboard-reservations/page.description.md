# Manage reservations

The working screen for front-desk staff: every reservation, with status changes through the booking lifecycle.

Columns, ordering and filters are open to change. Status transitions are validated on the server and must keep going through it. Guest contact details appear here and must never be exposed on a public page.
