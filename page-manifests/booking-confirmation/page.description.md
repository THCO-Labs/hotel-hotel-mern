# Booking confirmation

Shown immediately after a reservation is created. Its job is to make the booking reference impossible to miss, because that reference is how a guest finds the booking again through lookup.

Tone and layout may change; the reference must stay prominent and selectable. An unknown reference is a normal state and must render a clear not-found message.
