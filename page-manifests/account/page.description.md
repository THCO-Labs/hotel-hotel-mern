# Guest account

Lists the signed-in guest's reservations, most recent first.

The page sits behind an authentication guard and that guard must stay. Presentation of the list is open to change, and a guest with no bookings must get a helpful empty state rather than a blank panel. Only the signed-in guest's own reservations may ever appear here.
