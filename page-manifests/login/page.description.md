# Sign in

Email and password sign-in. Staff and guests use the same form; the role stored on the account decides what they can reach afterwards.

Presentation may change. The session is an HTTP-only cookie issued by the server - never move it into client storage, and never weaken the failure message to reveal whether an address is registered.
